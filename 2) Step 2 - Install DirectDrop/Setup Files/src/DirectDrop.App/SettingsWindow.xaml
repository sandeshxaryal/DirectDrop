<Window x:Class="DirectDrop.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DirectDrop settings" Width="650" Height="640" MinWidth="600" MinHeight="560"
        WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundBrush}" FontFamily="Segoe UI">
    <Grid Margin="28">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock x:Name="TitleText" Text="Settings" FontSize="25" FontWeight="SemiBold"/>
            <TextBlock x:Name="SubtitleText" Text="Choose where DirectDrop stores files on this PC." FontSize="13" Foreground="{StaticResource TextMutedBrush}" Margin="0,5,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,22,0,12"
                      VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled">
                <ScrollViewer.Resources>
                    <Style TargetType="ScrollBar">
                        <Setter Property="Width" Value="8"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Grid Background="Transparent" Width="8">
                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Background="Transparent" BorderThickness="0" IsTabStop="False"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb Margin="1,2,1,2" Background="{StaticResource BorderStrongBrush}" BorderThickness="0"/>
                                            </Track.Thumb>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Background="Transparent" BorderThickness="0" IsTabStop="False"/>
                                            </Track.IncreaseRepeatButton>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Resources>
            <StackPanel>
            <TextBlock Text="RECEIVED FILES" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
            <TextBlock Text="Files sent from your phone will be saved here." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox x:Name="UploadFolderTextBox" Grid.Column="0" Height="40" VerticalContentAlignment="Center" Padding="11,0" BorderThickness="1"/>
                <Button Grid.Column="1" Content="Browse…" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BrowseUploadButton_Click"/>
            </Grid>

            <TextBlock x:Name="ApplyHintText" Text="Folder changes apply to the next DirectDrop session." FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,14,0,0"/>

            <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,22,0,0">
                <StackPanel>
                    <TextBlock Text="ABOUT DIRECTDROP" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBlock Text="DirectDrop" FontSize="20" FontWeight="SemiBold" Margin="0,6,0,0"/>
                    <TextBlock Text="Built by Sandesh Aryal" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,3,0,0"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,10,0,0" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}">I built DirectDrop because I was frustrated with the ads, connection issues, and unreliable transfer experience of other alternatives. DirectDrop is designed for fast, direct local transfers without cloud upload requirements. It is open source.</TextBlock>
                    <TextBlock Text="GitHub" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}" Margin="0,12,0,3"/>
                    <Button x:Name="GitHubButton" Content="github.com/sandeshxaryal/directdrop" HorizontalAlignment="Left" Style="{StaticResource SecondaryButton}" Click="GitHubButton_Click"/>
                </StackPanel>
            </Border>

            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="ResetButton" Content="Reset defaults" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="ResetButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button x:Name="SaveButton" Content="Save" Style="{StaticResource PrimaryButton}" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
